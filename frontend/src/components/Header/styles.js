import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
    icon: {
        marginRight: '20px',
    },
    button1: {
        marginRight: '10px',
        marginLeft: '50%',
    },
    button2: {
        marginLeft: '25px',
        marginRight: '5px',
        background: 'white',

    }
}));

export default useStyles;